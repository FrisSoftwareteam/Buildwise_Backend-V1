{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "vercel-build": "pnpm --filter @workspace/api-server run build && cp Backend/api-server/dist/index.cjs ./index.cjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./Frontend/**\" --filter \"./Backend/**\" --filter \"./artifacts/mockup-sandbox\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "express": "^5"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
